## 1. Backend Config

- [x] 1.1 Normalize dotenv-provided CORS regex values before passing them to FastAPI
- [x] 1.2 Update backend `.env` guidance so the regex matches localhost and Netlify origins correctly

## 2. Verification

- [x] 2.1 Validate the OpenSpec change
- [x] 2.2 Run backend syntax verification
