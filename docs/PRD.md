# AI 模拟面试 MVP PRD

## 1. 项目定位

这是一个从 0 到 1 的 AI 模拟面试 MVP。产品要在尽量短的时间里，让用户完成一次有代入感的“闯关式”面试练习：选配置、开面、实时作答、拿反馈，再决定要不要马上再来一轮。

MVP 只验证一件事：

- 用户是否愿意用 AI 面试官或 AI 分身面试官完成一次完整练习

## 2. 产品目标

- 让用户在 3 分钟内走完一轮完整流程
- 让用户清楚理解“岗位 + 模式 + 面试官”三个配置维度
- 让结果页产出足够具体的反馈，驱动用户继续挑战下一轮

## 2.1 当前实现状态说明

本文档同时承担两个作用：

- 作为 MVP 目标定义，描述本版本希望达成的产品形态
- 作为当前实现状态参考，标记哪些能力已完成、部分完成、已调整范围或待补齐

状态标记说明：

- `已完成`：当前代码已经覆盖该能力
- `部分完成`：主链路可用，但仍有缺口
- `已调整范围`：能力存在，但已按最新设计收缩或改写
- `待补齐`：PRD 中有定义，但当前尚未落地

当前状态更新时间：`2026-04-27`

## 2.2 下一阶段工作计划

已确认的下一阶段工作重点如下：

- 多面试官真实联调
  - 系统面试官使用豆包完成真实外部联调
  - AI 分身面试官使用 SecondMe Visitor Chat 完成真实外部联调
  - 继续保留 legacy SecondMe 作为排障兜底，而不是主流程
- 管理台收口
  - 为 `/admin/interviewers` 补正式鉴权
  - 支持更稳的批量导入、操作审计和生产级权限边界
  - 让“问卷创建面试官”和“管理台编辑面试官”在体验上更一致
- 题库运营能力
  - 明确通用题库与面试官专属题库的维护边界
  - 优先补“专业题录入、查看、覆盖”的易用性
  - 后续再讨论题库比例混合、批量导入和审核机制
- 反馈页补全
  - 前端补展示 `dimensions`
  - 前端补展示 `suggestedAnswer`
  - 强化“再来一轮”主 CTA
- 部署与环境稳定性
  - 规范线上 `VITE_API_BASE_URL`
  - 继续验证 SQLite / Neon(Postgres) 持久化稳定性
  - 视情况升级到更正式的数据库与部署方案

补充说明：

- AI 分身面试官的目标接入方式是 `SecondMe Visitor Chat` 的开发者应用模式
- 终端用户不要求登录 SecondMe，但后端需要先完成开发者应用认证并持有 app token
- 当前代码已补 Visitor Chat 路径；测试期通过 `.env` 的 `SECONDME_AVATAR_API_KEY` 或数据库 `interviewer_profiles` 提供目标分身 key
- 应用内面试官 skill 由 Interview Hub 数据库维护，并在提问 prompt 中优先拼入；SecondMe 平台侧 skill 管理能力不作为当前阻塞项
- 当前已补隐藏管理台，可通过页面输入或上传 `.txt` / `.md` skill 文件，避免直接手写 SQL
- 当前已补数据库题库运行时：初版采用“通用开场题 + 专业题库 + 通用收尾题”的轻量模式，中间专业题优先查面试官专属题库，不足时回退到通用题库
- 当前问卷不再自动生成 interviewer-owned 题目；面试官需要自己上传或粘贴专属题库，初版建议直接录专业八股题即可
- 当前问卷已支持“面试官姓名 + 稳定用户名”，同一用户名重复提交时会更新同一位面试官和绑定题库
- 首页、设置页、面试页、反馈页现在已经对应真实地址：`/`、`/setup`、`/interview`、`/feedback`
- OAuth 登录检查仅用于验证 SecondMe 授权返回字段，不作为 MVP 面试主流程依赖

## 3. 范围定义

### 本版本必须实现

- 首页入口
- 岗位选择，固定 5 个岗位
- 模式选择，固定 2 种模式
- 面试官选择，支持系统面试官和 AI 分身面试官
- 面试流程，支持可配置轮次，目标范围为 1 到 10 轮，默认 3 轮
- 结果反馈，至少包含总结、3 条建议、参考优化方向
- 再来一轮，形成闭环
- 正式出题优先走数据库题库，而不是实时 AI 出题

当前状态：

- 首页入口：`已完成`
- 固定 5 个岗位：`已完成`
- 固定 2 种模式：`已完成`
- 面试官选择（系统面试官 + AI 分身面试官）：`部分完成`
  当前前后端已补系统面试官与 AI 分身面试官的 provider 骨架，并提供隐藏管理台、问卷页维护面试官资料与题库；豆包与 SecondMe Visitor Chat 仍待用真实凭证完成联调
- 面试流程（支持可配置轮次）：`已完成`
  当前前后端已支持显式 `1-10` 轮选择与统一校验，默认仍为 `3` 轮
- 结果反馈（总结、3 条建议、参考优化方向）：`部分完成`
  总结与建议已完成，反馈页已补每轮评价与参考答案；`dimensions` 与 `suggestedAnswer` 仍未完整展示
- 再来一轮闭环：`部分完成`
  流程已可重新开始，但反馈页仍缺少更明确的主 CTA 承接
- 正式出题走数据库题库：`已完成`
  当前运行时已收敛为“通用开场题 + 专业题库 + 通用收尾题”，不再把实时 AI 生成题目作为主链路

### 本版本不做

- 用户系统与登录鉴权
- 历史记录和成长曲线
- 语音输入输出
- 复杂评分算法
- 课程系统和 marketplace
- 多轮复杂面试编排

## 4. 核心用户流

1. 用户进入首页，被“开始挑战”CTA 引导进入设置页。
2. 用户在设置页选择岗位、模式和面试官。
3. 用户点击“开始挑战”，前端初始化面试 Session。
4. 用户进入真实模拟对话页，完成本轮选择的 1 到 10 轮问答。
5. 系统在面试结束后生成反馈，用户跳转到结果反馈页查看总结与建议。
6. 用户点击“再来一轮”，重新回到设置页继续练习。

补充说明：

- 当前前端路由已经和上述流程同步，不再只是 Zustand 阶段切换
- 当前面试出题已切到数据库，不再依赖上游 provider 即时生成每一道题
- 当前轻量流程为：
  - 第一题固定开场
  - 中间轮次抽取专业题
  - 最后一题固定收尾（总轮次大于等于 3）

## 5. 页面定义

### 5.1 首页 `Home`

页面目标：
作为产品入口，快速建立“AI 模拟面试”的价值感和挑战感。

核心功能：

- 展示产品标题、副标题和核心卖点
- 提供清晰的主 CTA：“开始挑战”
- 点击主 CTA 后跳转 `/setup`

当前状态：

- `已调整范围`
- 当前首页已按最新设计收缩为“一句文案 + 一个开始挑战按钮”
- 主 CTA 已完成并可进入设置页
- 副标题与卖点区未保留，属于有意简化而非遗漏

### 5.2 面试设置页 `Interview Setup`

页面目标：
让用户在最短路径内完成本轮挑战配置，并对本轮难度和风格形成预期。

核心功能：

- 岗位选择
  - 固定岗位：前端工程师、后端工程师、产品经理、运营、数据分析
- 模式选择
  - 引导模式 `guided`
  - 真实模式 `real`
- 面试官选择
  - 系统面试官：如技术面试官、HR 面试官
  - AI 分身面试官：先固定 2 个样例
- 轮次选择
  - 默认 `3` 轮
  - 允许用户选择 `1-10` 轮
- 配置摘要
  - 在 CTA 旁或 CTA 上方展示当前选择，减少误操作
- 开始挑战
  - 点击后调用初始化 Session 接口
  - 成功后携带 `sessionId` 和首个问题进入 `/interview`
- 异常提示
  - 配置不完整或接口失败时，展示明确提示并支持重试

建议状态结构：

```json
{
  "role": "frontend",
  "mode": "guided",
  "interviewerId": "system_tech"
}
```

当前状态：

- 岗位选择：`已完成`
- 模式选择：`已完成`
- 面试官选择：`部分完成`
  当前设置页已展示系统面试官与 AI 分身面试官两类入口，但外部 provider 仍待用真实凭证完成联调
- 配置摘要：`待补齐`
- 开始挑战并初始化 Session：`已完成`
- 异常提示：`已完成`
- 轮次配置：`已完成`
  设置页已支持显式 `1-10` 轮选择，前后端校验已同步
- 题库驱动配置感知：`部分完成`
  当前用户已能选择面试官并进入对应题库，但前端还没有更明确展示“该面试官使用专属题库还是通用题库”
- 路由与页面地址同步：`已完成`
  当前设置页固定地址为 `/setup`

### 5.3 真实模拟对话页 `Live Interview`

页面目标：
营造连续、专注、低打断的答题体验，让用户感觉自己真的在和面试官过招。

核心功能：

- 展示面试官信息、岗位、模式、当前轮次
- 以消息流形式展示问题和回答历史
- 提供回答输入框与发送按钮
- 发送回答后进入 loading 状态，避免重复提交
- 根据接口返回追加 AI 下一问
- 会话结束后进入“可查看反馈”的收口状态
- 异常情况下保留用户输入并允许重试

建议状态结构：

```json
{
  "sessionId": "uuid",
  "currentRound": 1,
  "totalRounds": 3,
  "messages": []
}
```

当前状态：

- 展示面试官信息、岗位、模式、当前轮次：`已完成`
- 回答输入框与发送：`已完成`
- 发送后的 loading 与防重复提交：`已完成`
- 根据接口返回追加下一问：`已完成`
- 收口到反馈页：`已完成`
- 异常情况下保留输入并支持重试：`部分完成`
- 以消息流形式展示完整历史：`已调整范围`
  当前主视图为专注答题模式，对话历史默认不直接展开，以贴合移动端设计方向
- 页面地址同步：`已完成`
  当前面试页固定地址为 `/interview`

### 5.4 结果反馈页 `Feedback Report`

页面目标：
把一次练习转化为可读、可执行、能驱动下一轮挑战的结果卡片。

核心功能：

- 总体评价
  - 1 段简洁总结，概括本轮表现
- 维度反馈
  - 至少 3 个维度，如表达清晰度、专业深度、问题贴合度
- 改进建议
  - 至少 3 条具体建议，避免空泛表述
- 参考优化方向
  - 给出更优回答思路或简短示例
- 再来一轮
  - 点击后返回设置页

建议数据结构：

```json
{
  "summary": "整体表达在线，但细节深度还可以继续补强。",
  "improvements": [
    "补足技术方案的取舍理由",
    "回答里增加量化结果",
    "结尾补一句总结会更稳"
  ],
  "suggestedAnswer": "如果重新回答，我会先讲背景，再讲方案、取舍和结果。"
}
```

当前状态：

- 总体评价：`已完成`
- 改进建议：`已完成`
- 每轮回答复盘：`已完成`
  这是在最新方案中新增强化的部分
- 维度反馈：`待补齐`
  后端已有维度数据，但前端当前未展示
- 参考优化方向：`部分完成`
  后端已有 `suggestedAnswer`，前端当前未完整展示
- 再来一轮：`部分完成`
  可重新开启流程，但页面仍缺少更明确的主 CTA 入口
- 页面地址同步：`已完成`
  当前反馈页固定地址为 `/feedback`

### 5.5 隐藏管理台 `Admin Interviewers`

页面目标：
作为内测期的运营后台，用来维护面试官资料、provider 配置、skill 和题库。

核心功能：

- 查看系统面试官和数据库面试官
- 新增、编辑、删除面试官
- 配置名称、描述、头像、标签、支持岗位、支持模式
- 录入 skill 文本和分身访问 key
- 维护面试官专属题库
- 按岗位维护通用兜底题库

当前状态：

- `已完成`
- 当前入口是隐藏地址 `/admin/interviewers`
- 当前已支持面试官 CRUD、专属题库和通用题库维护
- 当前仍缺少正式管理鉴权

### 5.6 面试官问卷 `Interviewer Questionnaire`

页面目标：
用更轻的填写方式，快速把一位真实面试官映射成应用内面试官配置。

核心功能：

- 录入面试官姓名
- 录入稳定用户名，作为题库归属与后续更新身份
- 选择公司风格、岗位、面试风格、考察点和流程模板
- 可选填写 SecondMe API Key，决定创建分身面试官还是系统面试官
- 支持上传或粘贴专属题库文本

当前状态：

- `已完成`
- 当前入口是隐藏地址 `/interviewer-questionnaire`
- 同一用户名再次提交时，会更新同一位面试官和它绑定的专属题库
- 当前建议：
  - 问卷主要录面试官信息和专业题
  - 通用开场题、收尾题由系统统一维护，不要求每位面试官重复填写

## 6. MVP 接口约束

- 前端以 `sessionId` 作为流程主键
- 初始化接口需要直接返回首个问题，减少额外请求
- 对话接口同时负责提交回答和接收 AI 回复
- 反馈接口可独立拉取，便于刷新页面后恢复结果
- MVP 默认无用户登录态，后续预留 `Authorization` 扩展位
- AI 分身面试官的目标接入方式是通过后端完成 `SecondMe Visitor Chat` 开发者应用认证，再以匿名访客身份访问目标分身
- 测试期目标分身由项目方手动生成 avatar API key 并配置到后端；当前可先通过隐藏管理台写入 `interviewer_profiles`，后续补正式后台权限后支持多分身维护
- 系统面试官与 AI 分身面试官允许使用不同的上游 provider 与不同的 Prompt 编排策略
- 管理台额外暴露通用题库接口，支持按岗位读取与覆盖 `formal_question_bank(scope=global)`
- 为避免 prompt 泄漏，SecondMe 实时通道返回内容会过滤内部 prompt 回显片段
- 面试题主链路以数据库选题为准，provider 不再承担“实时出题”的主职责

## 7. Definition of Done

- 设置页 -> 对话页 -> 结果页主流程可完整跑通
- 无阻塞性报错
- 每次练习都能得到完整结果反馈
- 用户可以从结果页无缝发起下一轮挑战

当前达成情况：

- 设置页 -> 对话页 -> 结果页主流程：`已完成`
- 无阻塞性报错：`部分完成`
  本地联调与部署环境配置仍需进一步稳定
- 每次练习都能得到完整结果反馈：`部分完成`
  本地真实链路可跑通，但仍依赖当前运行环境和 SecondMe 连通性
- 从结果页无缝发起下一轮挑战：`部分完成`
  能重开流程，但反馈页 CTA 仍建议补强
- 面试题可控、可复用、可运营：`部分完成`
  当前数据库题库已经接管主出题链路，但批量导入、审核、运营分析仍待补齐
