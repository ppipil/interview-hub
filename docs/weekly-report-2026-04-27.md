# Interview Hub 周报

时间范围：`2026-04-21` 至 `2026-04-27`

## 本周概览

本周重点是把产品从“能演示一次”推进到“可以持续配置和维护”。目前已经形成了从面试官录入、题库维护、面试进行到反馈复盘的完整闭环，后续可以更稳定地支持对外演示和内部验证。

## 本周完成

### 1. 主流程闭环完成

- 前端首页、设置页、面试页、反馈页已绑定真实地址：
  - `/`
  - `/setup`
  - `/interview`
  - `/feedback`
- 用户现在可以完整完成“选择配置 -> 开始面试 -> 查看反馈 -> 再来一轮”的主流程

### 2. 面试官配置能力补齐

- 明确拆分两类面试官：
  - 系统面试官：`doubao`
  - AI 分身面试官：`secondme_legacy / secondme_visitor`
- 隐藏管理台 `/admin/interviewers` 已支持：
  - 面试官 CRUD
  - skill / prompt / API key 维护
  - 面试官专属题库维护
  - 通用题库按岗位维护
- 已支持通过同一个后台持续更新面试官资料，不需要再手动改数据库

### 3. 面试官问卷已可用于调研录入

- 新增隐藏问卷页 `/interviewer-questionnaire`
- 支持录入：
  - 面试官姓名
  - 稳定用户名
  - 公司风格、岗位、面试风格、考察点、流程模板
  - SecondMe API Key
  - 专属题库文本
- 同一用户名再次提交时，会更新同一位面试官和它绑定的专属题库，不再每次生成一个新的 interviewer id
- 问卷已经调整为更贴近当前阶段的使用方式：面试官自己提供专业题，系统统一维护通用开场题和收尾题

### 4. 数据库题库正式接管出题

- 面试题主链路已切换为数据库唯一出题源
- 运行时规则为：
  - 开场题走通用题库
  - 中间专业题优先走面试官专属题库
  - 专属题不足时回退通用题库
  - 收尾题走通用题库
- 新增正式题库表和使用记录表：
  - `formal_question_bank`
  - `formal_question_usage`
- 这意味着当前面试题已经不再依赖实时 AI 生成，题目更可控，也更方便后续运营维护

### 5. 专业题库内容已补齐一版

- 后端岗位默认通用题库已替换为新的 6 组 60 题：
  - Java / 语言基础
  - MySQL
  - Redis
  - 计算机网络与系统
  - 分布式与微服务
  - 算法与数据结构
- 默认 `secondme_tech` 也同步更新了 3 道示例专属题，用于验证“专属题优先、通用题兜底”

### 6. 反馈页可读性提升

- 反馈页现在已经展示：
  - 总体总结
  - 改进建议
  - 每一轮问题
  - 候选人回答
  - 本轮评价
  - 参考答案
- “复盘观察”已下掉，页面信息更聚焦，便于用户直接看结论和参考答案

### 7. 联调和稳定性问题处理

- 修复了 SecondMe 实时链路里 prompt 回显到题目内容的问题
- 修复了 prompt 过滤过宽导致的 `502 Bad Gateway` 超时问题
- 验证了 Postgres / Neon 持久化启动链路，数据库模式可以正常完成初始化

## 验证结果

- 前端构建通过：`npm run build`
- 后端测试通过：`cd apps/api && ./.venv/bin/python -m unittest discover -s tests`
- 当前本地 API 已验证健康检查正常

## 当前风险和待补项

- 隐藏管理台仍无正式鉴权，不能直接暴露公网
- 豆包和 SecondMe Visitor Chat 仍依赖真实外部环境与测试凭证
- 反馈页还没有完整展示 `dimensions` 与 `suggestedAnswer`
- 运行时 session 仍是进程内状态，服务重启后无法续接
- 线上部署需要显式配置 `VITE_API_BASE_URL`

## 下周建议

- 完成豆包和 SecondMe Visitor Chat 的真实联调
- 为 `/admin/interviewers` 增加正式权限保护
- 继续优化题库录入和管理体验
- 补齐反馈页剩余展示项，并继续打磨对外演示效果
